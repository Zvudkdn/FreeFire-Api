services:
  - type: web
    name: flask-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    env: python
    region: oregon
    plan: free
    branch: main
    autoDeploy: true
